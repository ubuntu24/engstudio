require('dotenv').config({ path: '../.env' }); const { dbQueryAll } = require('./utils/db'); dbQueryAll('SELECT v.* FROM vocabulary v JOIN learning_progress lp ON lp.word_id = v.id WHERE lp.user_id = $1 AND lp.due_date <= $2 AND lp.status IN ('learning', 'reviewing', 'known') AND v.video_id IS NOT NULL AND v.video_id != '' ORDER BY lp.due_date ASC LIMIT $3', [1, '2026-07-29 09:18:00', 20]).then(console.log).catch(console.error);
